# LightAir — Claude Code notes

- Do NOT attempt to compile/build the project (e.g. `arduino-cli`, etc.)
  after making code changes, and do not treat a missing compiler as a
  blocker or something to report. Compiling/flashing is the user's
  responsibility. Only build if explicitly asked to.
- Verify changes by careful reading (Read/diff review) instead.
