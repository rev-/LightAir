═══════════════════════════════════════════════════
 WHAT'S GENUINELY MISSING
═══════════════════════════════════════════════════

(none outstanding)


═══════════════════════════════════════════════════
 DESIGN FLAWS
═══════════════════════════════════════════════════

1. Totem assignment encoding is a hack
   _totemAssignment[] in GameSetupMenu encodes two conceptually different
   things in one byte: values < GenericTotemRoles::COUNT (3) = generic
   role, values ≥ 3 = totemVar index offset by 3. This is fragile —
   adding a new generic role breaks all existing totemVar indices silently.
   Should be a struct: { bool isNamed; uint8_t value; }.

2. Enlight via global pointer
   Games declare extern Enlight* enlightPtr (e.g. GameFreeForAll.cpp ~47).
   The pointer is initialized in the sketch, but nothing prevents a game
   from dereferencing it before initialization, and there's zero
   compile-time safety. Enlight should be injected into GameRunner::begin()
   or into the game callbacks as a parameter.

3. [DONE] Message type collisions with no registry
   Resolved: all MSG_* constants now live in a single RadioMsg namespace
   in config.h. typeId filtering still isolates game sessions at runtime,
   but the central registry prevents accidental value overlap across games.

4. Config blob truncation is silent
   game_apply_config() has no total-length check. A truncated blob
   (e.g. partial radio delivery) passes the typeId check and is
   deserialized by fixed offset arithmetic, silently producing garbage
   configVar / totemVar values. The function returns false only on
   typeId mismatch or buffer shorter than 2 bytes.


═══════════════════════════════════════════════════
 SUGGESTIONS
═══════════════════════════════════════════════════

#   Suggestion                                                        Priority
─────────────────────────────────────────────────────────────────────────────
5   [DONE] typeId uniqueness enforced in registerGame(); central
    GameTypeIds.h registry added; all game descriptors use named
    constants; AllGames.cpp asserts on collision at boot.

6   Replace _totemAssignment[] encoding hack with a struct           Medium
    { bool named; uint8_t value; }

7   Inject Enlight* into GameRunner::begin() instead of global       Low
    pointer; pass to callbacks

8   Totem runners partially implemented                              Medium

    Teams — BaseRunner: onMessage() handles respawn (0xF1 → Respawn
    UI event). No update(), no onRoster() override. Fine as-is only if
    the base never needs to broadcast or aggregate round-end data.

    Flag — FlagCompositeRunner: onMessage() covers BASE (respawn) and
    FLAG (state-change animations). No update(): the flag totem never
    broadcasts its own state periodically, which may be intentional but
    means players have no unsolicited flag-position feedback. No
    onRoster() override: flag totems send no capture summary at round end.

    Upkeep — UpkeepCompositeRunner: most complete. update() handles
    the full CP beacon / presence-window / scoring cycle. Still no
    onRoster() override; CP totems send no final-score contribution.

    All runners — onRoster() defaults to base (empty reply + Roster UI
    event). If totems should report per-device stats at game end
    (captures, CP ticks, etc.) this needs to be overridden per game.
